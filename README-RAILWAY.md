# Railway Configuration
# Railway will detect Node.js version from:
# 1. .nvmrc file (this file)
# 2. package.json "engines.node" field
# 3. Environment variable NODE_VERSION

# Force Node.js 20 LTS
NODE_VERSION=20

